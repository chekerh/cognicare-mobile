name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend

    services:
      mongodb:
        image: mongo:7.0
        ports:
          - 27017:27017

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Run tests
        run: npm run test:cov
        env:
          MONGODB_URI: mongodb://localhost:27017/cognicare_test

      - name: Build application
        run: npm run build

  frontend-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      - name: Cache Flutter dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.pub-cache
          key: ${{ runner.os }}-flutter-${{ hashFiles('frontend/pubspec.yaml') }}
          restore-keys: |
            ${{ runner.os }}-flutter-

      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            android/.gradle
          key: ${{ runner.os }}-gradle-${{ hashFiles('android/gradle/wrapper/gradle-wrapper.properties', 'android/build.gradle.kts', 'android/app/build.gradle.kts') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Clean and install dependencies
        run: |
          flutter --version
          rm -f pubspec.lock
          flutter pub get

      - name: Run Flutter analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Run Flutter tests
        run: flutter test

      - name: Fix record_android compileSdk for AGP
        run: |
          set -e
          RECORD_ANDROID_BUILD=$(find "$HOME/.pub-cache" -path "*record_android*android/build.gradle" 2>/dev/null | head -1)
          if [ -z "$RECORD_ANDROID_BUILD" ] || [ ! -f "$RECORD_ANDROID_BUILD" ]; then
            echo "::error::record_android build.gradle not found"
            exit 1
          fi
          echo "Patching $RECORD_ANDROID_BUILD"
          cp "$RECORD_ANDROID_BUILD" "$RECORD_ANDROID_BUILD.bak"
          # Replace every flutter.* so no 'flutter' property is needed
          sed -i 's/flutter\.compileSdkVersion/35/g' "$RECORD_ANDROID_BUILD"
          sed -i 's/flutter\.minSdkVersion/23/g' "$RECORD_ANDROID_BUILD"
          sed -i 's/flutter\.targetSdkVersion/35/g' "$RECORD_ANDROID_BUILD"
          sed -i 's/flutter\.ndkVersion/"27.0.12077973"/g' "$RECORD_ANDROID_BUILD"
          # Remove any remaining flutter.xxx (fallback to 35)
          sed -i 's/flutter\.[a-zA-Z]*/35/g' "$RECORD_ANDROID_BUILD"
          if grep -q "flutter\." "$RECORD_ANDROID_BUILD"; then
            echo "::warning::Some flutter. refs remain, re-patching"
            sed -i 's/flutter\.[a-zA-Z0-9.]*/35/g' "$RECORD_ANDROID_BUILD"
          fi
          if ! grep -qE "compileSdk|compileSdkVersion" "$RECORD_ANDROID_BUILD"; then
            sed -i '/android {/a\    compileSdkVersion 35' "$RECORD_ANDROID_BUILD"
          fi
          echo "Patched android block:"
          sed -n '/^android {/,/^}/p' "$RECORD_ANDROID_BUILD" || true

      - name: Build Android APK
        run: flutter build apk --debug

      - name: Build Web
        run: flutter build web

  docker-build:
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-test]
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push backend Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/cognicare-backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push frontend Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/cognicare-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    runs-on: ubuntu-latest
    needs: docker-build
    if: github.ref == 'refs/heads/main'
    environment: production

    steps:
      - name: Deploy to production
        run: |
          echo "Deploying to production..."
          # Add your deployment commands here
          # This could be Docker Compose, Kubernetes, etc.